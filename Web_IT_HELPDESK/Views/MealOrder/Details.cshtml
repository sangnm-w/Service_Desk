@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

@using (Html.BeginForm("MealOrderPrint", "MealOrder", FormMethod.Post))
{
<div>
    <input type="radio" name="reportid" value="PDF"/ checked="checked"> <label>PDF Report</label>
    <input type="radio" name="reportid" value="Excel"/> <label>Excel Report</label>
    <input type="radio" name="reportid" value="Word"/> <label>Word Report</label>
    <input type="radio" name="reportid" value="Image"/> <label>Image Report</label>
</div>
<input type="submit" value="Print" /> 
}

@using (Html.BeginForm("", "", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    int j = 1;
    int oderId=Convert.ToInt32(Session["OrderId"].ToString());
<table>
    <tr>
        <th>
        </th>      
        <th>
            Id - Mã
        </th>
        <th>
            Item - Tên
        </th>
        <th>
            Qty - Số lượng
        </th>
         <th>
            Note - Ghi chú
        </th>
    </tr>
@foreach (var item in new Web_IT_HELPDESK.ServiceDeskEntities().OrderDetails.Where(i => i.OrderId == oderId))
{
    <tr>
        <td>
              @Html.Hidden("mealorderdetails.Index", (@j+10))
              @Html.TextBox("mealorderdetails[" + (@j + 10) + "].OrderDetailId", item.OrderDetailId, new {@readonly="readonly",@hidden="hidden"})
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Album.Title)
        </td>
        <td>
             @Html.DisplayFor(modelItem =>item.Quantity)        
        </td>
        <td>
            @Html.DisplayFor(modelItem =>item.Note)     
        </td>
        <!--<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderDetailId })
        </td>-->
    </tr>
    j = (Int16)j + 1;
}

</table>
}